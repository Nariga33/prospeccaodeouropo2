# Retirar emojis dos cartões de exemplo (Mentalidade/Consciência)

Trocar os emojis ❌ e ✅ por marcadores tipográficos com tokens do design system, no `EvolutionModel.tsx`:

- ❌ → tag pequena "ANTES" com `text-red-300/90` + ponto/traço dourado
- ✅ → tag pequena "DEPOIS" com `text-gold` + ponto/traço dourado

Layout: cada exemplo vira duas linhas com label em uppercase tracking-wide à esquerda e a frase ao lado, separadas por um divisor sutil. Sem emojis em nenhum lugar do componente.

Nenhum outro arquivo é alterado.
