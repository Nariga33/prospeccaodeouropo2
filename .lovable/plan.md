Aumentar a logo do menu (header) de `h-9` (36px) para `h-11` (44px), um aumento de ~22% que dá mais destaque sem exagerar. Ajustar o espaçamento/padding do header se necessário para manter o alinhamento visual.

### Arquivo a alterar
- `src/components/po2/Nav.tsx` — alterar `className="h-9 w-auto"` para `className="h-11 w-auto"` na imagem da logo.

Nenhuma outra mudança necessária.